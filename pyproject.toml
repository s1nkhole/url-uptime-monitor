[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008: Depends(...) in argument defaults is the standard FastAPI DI pattern.
ignore = ["B008"]
